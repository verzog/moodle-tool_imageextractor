define("tool_imageextractor/jobtype",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * Show only the job-form sections relevant to the selected job type.
   *
   * The form's hideIf rules hide the individual fields, but Moodle's form
   * dependencies cannot hide section headers, which would linger as empty
   * fieldsets. This module toggles the whole type-specific sections: the
   * output section is extract-only, the replacement section replace-only.
   *
   * @module     tool_imageextractor/jobtype
   * @copyright  © Skin Cancer College Australasia
   * @license    Proprietary — Skin Cancer College Australasia, all rights reserved
   */
const SECTIONS={extract:["id_outputheader"],replace:["id_replacementheader"]},toggle=(id,show)=>{const fieldset=document.getElementById(id);fieldset&&fieldset.classList.toggle("d-none",!show)},apply=field=>{const type="replace"===field.value?"replace":"extract";SECTIONS.extract.forEach((id=>toggle(id,"extract"===type))),SECTIONS.replace.forEach((id=>toggle(id,"replace"===type)))};_exports.init=()=>{const field=document.querySelector('form [name="jobtype"]');field&&(field.addEventListener("change",(()=>apply(field))),apply(field))}}));

//# sourceMappingURL=jobtype.min.js.map